[package]
name = "bevy_rust_meetup"
version = "0.1.0"
edition = "2021"

# Enable optimizations for dependencies but not for our code
[profile.dev.package."*"]
debug = false
opt-level = 1

[dependencies]
bevy = "0.12.1"
